import { FC } from "react";

// Layout
import Layout from "../layout";

const Category: FC = () => {
  return (
    <Layout>
      <div>This is category</div>
    </Layout>
  );
};

export default Category;
