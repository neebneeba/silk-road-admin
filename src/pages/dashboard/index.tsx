import { FC } from "react";

const Dashboard: FC = () => {
  return (
    <div className="h-screen flex">
      <div className="m-auto">this is Dashboard</div>
    </div>
  );
};

export default Dashboard;
